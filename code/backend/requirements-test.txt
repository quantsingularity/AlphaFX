# AlphaFX - Minimal test requirements
# Installs only what is needed to run the 88-test suite.
# No Redis, no PostgreSQL, no daphne required.
# Tests use SQLite + LocMemCache automatically when optional packages are absent.
#
# Install with:
#   pip install -r requirements-test.txt

Django>=5.0,<5.3
djangorestframework>=3.15,<3.16
djangorestframework-simplejwt>=5.3,<5.4
django-cors-headers>=4.3,<4.5
django-filter>=24.2,<25.0
django-environ>=0.11,<0.12
dj-database-url>=2.1,<3.0
drf-spectacular>=0.27,<0.28
channels>=4.1,<4.2
scipy>=1.13,<2.0
numpy>=1.26,<2.1
pandas>=2.2,<2.3
httpx>=0.27,<0.28
whitenoise>=6.7,<7.0
pytest>=8.0
pytest-django>=4.8
