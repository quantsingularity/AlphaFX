apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-models-pvc
  namespace: alphafx
  labels:
    app.kubernetes.io/name: alphafx
    app.kubernetes.io/component: ai-services
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
