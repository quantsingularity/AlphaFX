# IMPORTANT: Replace all placeholder values before deploying to any environment.
# Generate a secure Django secret key:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())" | base64
# Encode values:
#   echo -n "your-value" | base64
#
# For production, prefer a secrets manager (e.g. AWS Secrets Manager, Vault,
# or Kubernetes External Secrets Operator) instead of committing this file.
apiVersion: v1
kind: Secret
metadata:
  name: alphafx-secret
  namespace: alphafx
  labels:
    app.kubernetes.io/name: alphafx
    app.kubernetes.io/component: secret
type: Opaque
data:
  # echo -n "change-me-to-a-long-random-secret-key" | base64
  SECRET_KEY: Y2hhbmdlLW1lLXRvLWEtbG9uZy1yYW5kb20tc2VjcmV0LWtleQ==

  # echo -n "alphafx_secure_password" | base64
  DB_PASSWORD: YWxwaGFmeF9zZWN1cmVfcGFzc3dvcmQ=

  # echo -n "postgresql://alphafx:alphafx_secure_password@postgres:5432/alphafx" | base64
  DATABASE_URL: cG9zdGdyZXNxbDovL2FscGhhZng6YWxwaGFmeF9zZWN1cmVfcGFzc3dvcmRAcG9zdGdyZXM6NTQzMi9hbHBoYWZ4

  # External API keys (optional -- synthetic fallback is used when empty)
  # echo -n "your-exchangerate-api-key" | base64
  EXCHANGERATE_API_KEY: ""

  # echo -n "your-alpha-vantage-key" | base64
  ALPHA_VANTAGE_KEY: ""
